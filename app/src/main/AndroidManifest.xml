<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- Phase 7: Supabase auth/sync (SupabaseAuthRepository, SyncEngine) needs real
         network access; nothing before this phase made any network calls. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Normal permission, no runtime request needed — for Rule D's
         haptic cueing during calibration (visual + haptic only, no
         audible click competing with the sweep/mic). -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- No android:icon yet — Phase 0 has no app icon asset. Android falls back to a
         system default launcher icon until Phase 8 (or earlier, whenever branding lands). -->
    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:theme="@style/Theme.SongNotes"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.SongNotes">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".RecordingForegroundService"
            android:foregroundServiceType="microphone"
            android:exported="false" />
    </application>

</manifest>
